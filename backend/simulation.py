def run_rocketpy(params: dict, lat: float, lon: float, elevation: float,
                 rail_length: float, inclination: float, heading: float,
                 use_live_weather: bool, output_dir: str) -> dict:
    """Run RocketPy simulation from parameters.json dict. Returns results dict."""
    raise NotImplementedError
